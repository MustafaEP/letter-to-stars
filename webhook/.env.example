PORT=9000
HOST=0.0.0.0

# GitHub Webhook Secret (HMAC) — GitHub repo settings > Webhooks > Secret
WEBHOOK_SECRET=

# Only deploy on this branch
DEPLOY_BRANCH=main

# Deploy script path (inside container via /opt/letter-to-stars mount)
DEPLOY_SCRIPT=/opt/letter-to-stars/scripts/deploy.sh

# Where webhook appends deploy logs
WEBHOOK_DEPLOY_LOG=/opt/letter-to-stars/logs/webhook-deploy.log

